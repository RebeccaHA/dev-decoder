{
  "name": "dev-decoder",
  "license": "MIT",
  "engines": {
    "node": "10.15.3"
  },
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cd ..",
    "deploy": "cp -a frontend/build/. public/",
    "heroku-postbuild": "npm deploy"
  }
}
